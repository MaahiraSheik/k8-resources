apiVersion: v1
kind: Pod
metadata:
  name: resources
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests: #soft limit
        memory: "100mi"
        cpu: "400m"
      limits: # Hard limit
        memory: "128Mi"
        cpu: "500m"
